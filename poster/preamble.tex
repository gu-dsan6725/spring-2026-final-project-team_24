% Poster preamble (loaded via include-in-header: file: preamble.tex).
% Lifted out of the YAML `text: |` block as a matter of organization;
% any LaTeX macros and titlesec configuration now live here.

\usepackage{multicol}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage{qrcode}
\usepackage[scaled=0.92]{helvet}
\renewcommand{\familydefault}{\sfdefault}

\definecolor{ggblue}{HTML}{0B3061}
\definecolor{ggdeep}{HTML}{05203F}
\definecolor{gggold}{HTML}{8C6F2C}
\definecolor{soft}{HTML}{F1F5F9}
\definecolor{accent}{HTML}{1E3A8A}
\definecolor{ok}{HTML}{166534}
\definecolor{rowalt}{HTML}{F3F6FB}

\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.45em}
\setlength{\columnsep}{0.42in}
\setlength{\columnseprule}{0.4pt}
\setlist[itemize]{leftmargin=1.3em,topsep=2pt,itemsep=3pt,parsep=0pt}
\setlist[enumerate]{leftmargin=1.5em,topsep=2pt,itemsep=3pt,parsep=0pt}

% --- Section heading rule ---------------------------------------------------
% Wrapped in a named macro so the after-code in \titleformat is a single
% token group; defensive against any future re-processing.
\newcommand{\posterSectionRule}{%
  \vspace{-0.35em}\textcolor{ggblue}{\rule{\linewidth}{1.2pt}}%
}

\titleformat{\section}{\Huge\bfseries\color{ggblue}}{}{0em}{}[\posterSectionRule]
\titlespacing*{\section}{0pt}{0.6em}{0.4em}

\titleformat{\subsection}{\LARGE\bfseries\color{accent}}{}{0em}{}
\titlespacing*{\subsection}{0pt}{0.6em}{0.2em}
